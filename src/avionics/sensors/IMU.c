// get the sensor data
// update the struct for the values of each part
// act as a callable function

typedef struct imu_raw{
    float omega[3];
    float acc[3];
}imu_raw;

void update(imu_raw state_raw){
    // update state_raw.omega

    // update state_raw.acc
}
